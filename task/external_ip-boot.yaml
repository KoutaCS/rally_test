---
  version: 2
  title: Nipa.Cloud attach ip test
  description: test by boot and attach ip to instance and delete
  subtasks:
    -
      title: Nipa.Cloud boot and attach ip to it
      workloads:
        {% for image in images %}
        {% for flavor in flavors %}
        -
          scenario: 
            NipaCloud.boot_associate_dissociate_floating_ip_and_delete:
              image:
                name: {{image|e}}
              flavor:
                name: {{flavor|e}}
          runner:
            constant:
              times: {{times}}
              concurrency: {{concurrency}}
          contexts:
            network: {}
          sla:
            failure_rate:
              max: 0
        {% endfor %}
        {% endfor %}