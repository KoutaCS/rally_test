---
  version: 2
  title: Nipa.Cloud Continue Billing Test
  description: Test by Boot instance, Pause And Lock it and unlock and unpause then Delete
  subtasks:
    -
      title: Nipa.Cloud continue payment
      workloads:
        {% for image in images %}
        {% for flavor in flavors %}
        -
          scenario:
            NipaCloud.billing_continue_payment:
              image:
                name: {{image|e}}
              flavor:
                name: {{flavor|e}}
              force_delete: false
          runner:
            constant:
              times: {{times}}
              concurrency: {{concurrency}}
          contexts: {}
          sla:
            failure_rate:
              max: 0
        {% endfor %}
        {% endfor %}